{
  "name": "@functional-cqrs/stores",
  "version": "0.1.0",
  "description": "Manages stores that contain handlers",
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "public"
  },
  "main": "build/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/TheUnderScorer/functional-cqrs.git"
  },
  "scripts": {
    "dev": "tsc -p tsconfig.build.json -w",
    "build": "yarn run clean && tsc -p tsconfig.build.json",
    "prod": "node build/index.js",
    "clean": "rm -rf build .cache",
    "test": "jest --config ../../jest.config.js"
  },
  "dependencies": {
    "@functional-cqrs/typings": "0.1.0"
  }
}
